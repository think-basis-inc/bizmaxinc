# Public build-time variables (safe to commit). Copy to .env for local builds.
# Meta Business Manager -> Brand Safety -> Domains -> bizmaxinc.com -> Meta-tag verification code.
PUBLIC_META_DOMAIN_VERIFICATION=
